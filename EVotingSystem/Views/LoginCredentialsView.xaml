<UserControl x:Class="EVotingSystem.Views.LoginCredentialsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

	<StackPanel Margin="40" Width="250" HorizontalAlignment="Center">

		<TextBlock Text="EVoting"
                   FontSize="32"
                   FontWeight="Bold"
                   HorizontalAlignment="Center"
                   Margin="0,0,0,30"/>

		<TextBlock Text="Korak 2: Kredencijali"
                   FontSize="16"
                   FontWeight="SemiBold"
                   HorizontalAlignment="Center"
                   Margin="0,0,0,20"/>

		<TextBlock Text="Korisnicko ime"/>
		<TextBox x:Name="UsernameTextBox"
                 Height="25"
                 Margin="0,5,0,15"
                 VerticalContentAlignment="Center"/>

		<TextBlock Text="Lozinka"/>
		<PasswordBox x:Name="PasswordBox"
                     Height="25"
                     Margin="0,5,0,20"
                     VerticalContentAlignment="Center"/>

		<Button Content="Prijavi Se"
                Height="35"
                Margin="0,0,0,15"
                Click="Login_Click"/>

		<TextBlock HorizontalAlignment="Center">
            <Run Text="Nemate profil? "/>
            <Run Text="Registrujte se!"
                 Foreground="Blue"
                 TextDecorations="Underline"
                 Cursor="Hand"
                 MouseLeftButtonDown="Register_Click"/>
		</TextBlock>

	</StackPanel>
</UserControl>
