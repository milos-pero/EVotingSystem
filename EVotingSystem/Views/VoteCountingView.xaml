<UserControl x:Class="EVotingSystem.Views.VoteCountingView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

	<Grid Margin="30">
		<Grid.RowDefinitions>
			<RowDefinition Height="Auto"/>
			<RowDefinition Height="*"/>
		</Grid.RowDefinitions>

		<!-- HEADER -->
		<StackPanel Margin="0,0,0,20">
			<TextBlock Text="Prebrojavanje glasova"
                       FontSize="26"
                       FontWeight="Bold"/>

			<TextBlock Text="{Binding Title}"
                       FontSize="18"
                       FontWeight="Bold"
                       Margin="0,8,0,0"/>

			<TextBlock Text="{Binding Description}"
                       Foreground="Gray"
                       TextWrapping="Wrap"/>
		</StackPanel>

		<!-- RESULTS -->
		<StackPanel Grid.Row="1">

			<TextBlock Text="{Binding TotalVotesText}"
                       FontSize="14"
                       FontWeight="Bold"
                       Margin="0,0,0,10"/>

			<DataGrid ItemsSource="{Binding Results}"
                      AutoGenerateColumns="False"
                      IsReadOnly="True"
                      CanUserAddRows="False"
                      HeadersVisibility="Column">

				<DataGrid.Columns>
					<DataGridTextColumn Header="Opcija"
                                        Binding="{Binding Option}"
                                        Width="*"/>

					<DataGridTextColumn Header="Broj glasova"
                                        Binding="{Binding Count}"
                                        Width="150"/>
				</DataGrid.Columns>

			</DataGrid>

		</StackPanel>
	</Grid>
</UserControl>
