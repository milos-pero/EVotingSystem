<UserControl x:Class="EVotingSystem.Views.OrganizerMainView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

	<Grid Margin="40">
		<StackPanel HorizontalAlignment="Center"
                    VerticalAlignment="Center"
                    Width="300">

			<TextBlock Text="Organizer panel"
                       FontSize="26"
                       FontWeight="Bold"
                       HorizontalAlignment="Center"
                       Margin="0,0,0,30"/>

			<Button Content="Kreiraj novo glasanje"
                    Height="45"
                    FontSize="14"
                    Margin="0,0,0,15"
                    Click="CreateVoting_Click"/>

			<Button Content="Pregled glasanja"
                    Height="45"
                    FontSize="14"
                    Click="ViewVotings_Click"/>

		</StackPanel>
	</Grid>
</UserControl>
